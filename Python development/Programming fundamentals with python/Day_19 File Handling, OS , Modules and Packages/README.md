# Python Daily Practice 🚀🐍

## 📅 Today's Learning

Today, I practiced important Python concepts related to **File Handling, CSV Files, OS Operations, and Python Modules**.

---

## 📂 File Handling

* Practiced working with different file modes.
* Learned about **binary file handling**.
* Understood **exclusive file creation mode (`x`)**.
* Explored file operations and their practical usage.

---

## 📊 CSV File Handling

* Created and wrote data into **CSV files**.
* Read CSV files using Python.
* Practiced working with the **`csv` module**.
* Displayed CSV data row by row.

---

## 💻 OS Module

Practiced using Python's **`os` module** to interact with files and directories.

### Operations Practiced:

* Checking the current working directory using `os.getcwd()`
* Checking whether a file exists using `os.path.isfile()`
* Renaming files using `os.rename()`
* Deleting files using:

  * `os.remove()`
  * `os.unlink()`
* Creating directories using `os.mkdir()`
* Removing directories using `os.rmdir()`

---

## 📦 Python Modules

Today, I also practiced creating and using **custom Python modules**.

### Module Creation

Created a custom module containing:

* A variable
* A function for multiplication

### Different Ways of Importing Modules

Practiced multiple ways to import and use modules:

* `import module_name`
* Importing a module with an **alias**
* Importing specific components using `from module import ...`
* Importing all components using `from module import *`
* Aliasing imported variables and functions

### Working with Classes from Modules

* Imported a **class from another module**
* Created an object of the imported class
* Called a method using that object

---

## 🛠️ Modules and Libraries Used

* `csv`
* `os`
* `shutil`

---

## 💡 Key Takeaways

Today's practice helped me understand how Python can:

* Work with files and directories
* Handle CSV data
* Interact with the operating system
* Create reusable custom modules
* Import modules and their components in different ways
* Reuse classes and functions across multiple Python files

---

### 🚀 Daily Python Practice Journey

Consistently learning and practicing Python every day, building strong programming fundamentals one concept at a time! 🐍💻

**#Python #PythonLearning #100DaysOfCode #CodingJourney**
