# 🐍 Python Daily Practice — Day 10

Day 10 of my daily Python programming practice focused on strengthening my understanding of **loops, conditional statements, strings, lists, and basic problem-solving**.

## 📚 Topics & Programs Covered

### 1. Prime Number Checker
Program to check whether a given number is prime or not.

**Concepts:**
- `if-else`
- `for` loop
- `range()`
- Modulus operator `%`
- Square root optimization
- `break`
- `for-else`

### 2. String Transformation
Convert:

`python is easy`

into:

`nythop si yase`

The program splits the string into words and rearranges the characters of each word.

**Concepts:**
- `split()`
- String indexing
- String slicing
- Lists
- `join()`
- `for` loop

### 3. Convert a List into a List of Squares
Convert:

`[1, 2, 3, 4]`

into:

`[1, 4, 9, 16]`

**Concepts:**
- Lists
- `for` loop
- `append()`
- Exponentiation

### 4. Factorial of a Number
Program to calculate the factorial of a number using a `for` loop.

**Concepts:**
- `for` loop
- `range()`
- Multiplication
- Accumulator variable

### 5. Mean of a List
Program to calculate the mean of a list of numbers.

**Concepts:**
- Lists
- `for` loop
- Sum calculation
- `len()`
- Arithmetic operations

---

## 🔄 While Loop Practice

### 6. Display Numbers from 20 to 0
Print numbers from `20` down to `0` using a `while` loop.

### 7. Display Numbers from 0 to 10
Print numbers from `0` to `10` using a `while` loop.

### 8. Sum from 1 to N
Take a number from the user and calculate the sum of all numbers from `1` to `N`.

### 9. Sum of List Elements
Calculate the sum of all elements in a list using a `while` loop.

### 10. Display Even Numbers
Print all even numbers between `0` and `20` using a `while` loop.

### 11. Display Odd Numbers
Print all odd numbers between `0` and `20` using a `while` loop.

### 12. Palindrome Checker
Check whether a given string is a palindrome using a `while` loop.

**Concepts:**
- String indexing
- Negative indexing
- `len()`
- `while` loop
- `break`
- `while-else`

---

## 🔁 While-Else Practice

### 13. While-Else Without Break
Demonstration of how the `else` block executes when a `while` loop completes successfully without interruption.

### 14. While-Else with Break
Demonstration of how using `break` prevents the `else` block of a `while` loop from executing.

---

## 🛠️ Concepts Practiced

- Variables and input
- `if-else`
- `for` loops
- `while` loops
- `for-else`
- `while-else`
- `break`
- `range()`
- Lists
- String manipulation
- String indexing and slicing
- `split()`
- `join()`
- `append()`
- `len()`
- Modulus operator
- Mathematical operations
- Basic problem-solving

## 🎯 Goal

The goal of this daily practice is to build strong Python fundamentals through consistent hands-on problem solving and gradually improve logical thinking and programming skills.

## 📅 Progress

**Day 10 ✅**

Continuing the journey of learning Python one problem at a time.